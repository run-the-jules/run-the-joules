<!DOCTYPE html>
<html>
  <head>
    <title>RunTheJoules</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body class="bg-gray-500">
    <main class="site-main px-6">
      <div class="flex flex-col-h-screen">
        <section class="mb-24 -mt-2">
          <ul class="grid grid-gutter grid-flow-col col-start-1 col-end-auto grid-cols-max-content gap-3 list-none md:flex md:flex-wrap -mx-6 md:mx-0 py-1 md:py-2 overflow-x-auto md:overflow-x-visible scrolling-touch scrollbar-hidden " >
            <li class="flex-none inline-flex">
              <%= link_to "Log In", login_path, class: "button rounded-lg py-1 md:py-2 px-5 bg-blue-200 hover:bg-blue-500 hover:text-white focus:bg-blue-500 focus:text-white transition duration-150-ease-in-out" %><br>
            </li>
            <li class="flex-none inline-flex">
              <%= link_to "Log Out", logout_path, class: "button rounded-lg py-1 md:py-2 px-5 bg-blue-200 hover:bg-blue-500 hover:text-white focus:bg-blue-500 focus:text-white transition duration-150-ease-in-out" %><br>
            </li>
            <li class="flex-none inline-flex">
              <%= link_to "Register", registration_path, class: "button rounded-lg py-1 md:py-2 px-5 bg-blue-200 hover:bg-blue-500 hover:text-white focus:bg-blue-500 focus:text-white transition duration-150-ease-in-out" %>
            </li>
            <li class="flex-none inline-flex">
              <%= link_to "Home", root_path, class: "button rounded-lg py-1 md:py-2 px-5 bg-blue-200 hover:bg-blue-500 hover:text-white focus:bg-blue-500 focus:text-white transition duration-150-ease-in-out" %>
            </li>
          </ul>
        </section>
      </div>
      <%= yield %>
      <section id="flash-messages" >
        <% flash.each do |notice, messages| %>
          <% if messages.class == Array %>
            <p><%= messages.to_sentence %></p>
          <% else %>
            <p><%= messages%></p>
          <% end %>
        <% end %>
      </section>
      <div class="flex flex-col-h-screen">
        <section class="mb-24 -mt-2">
        <ul class="grid grid-gutter grid-flow-col col-start-1 col-end-auto grid-cols-max-content gap-3 list-none md:flex md:flex-wrap -mx-6 md:mx-0 py-1 md:py-2 overflow-x-auto md:overflow-x-visible scrolling-touch scrollbar-hidden " >
        <li class="flex-none inline-flex">
          <%=  'Contact Us' %>
        </li>
        <li class="flex-none inline-flex">
          <%= 'About Us' %>
        </li>
        <li class="flex-none inline-flex">
          <a href="https://utilityapi.com">Utility Api</a>
        </li>
    </main>
  </body>
</html>
